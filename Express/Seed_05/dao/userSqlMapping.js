var user = {
    insert: 'INSERT INTO tc_common_func_apply (task_id, applyer, apply_reason, state) VALUE (?, ?, ?, ?)',
    update: '',
    delete: '',
    queryById: '',
    queryAll: ''
};

module.exports = user;
